/*
 * Sbox 2x1 int dynamic actor
 * (generated by the MDC tool)
 */

package it.diee.unica.multidec;

 actor Sbox2x1int8 (int SIZE = 8) bool sel, int(size=SIZE)in1, int(size=SIZE)in2 ==> int(size=SIZE)out1:

  bool selector := false;

  action sel:[s] ==> 
 do
 	selector := s;
 end

action in1: [a] ==> out1: [a]  guard not selector
 end

action in2: [a] ==> out1: [a]  guard selector
 end


end